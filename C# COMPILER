using System;

class Program
{
    static void Main()
    {
        
        int age = 20;

        
        string name = "Andrea";

        
        bool isLesbian = true;


        
        double price = 75.99;

        
        Console.WriteLine(age);
        Console.WriteLine(name);
        Console.WriteLine(isLesbian);
        Console.WriteLine(price);
    }
}





using System;

class Program
{
    static void Main()
    {
        int age = 20;
        string name = "Andrea";
        bool isLesbian = true;
        double price = 75.99;

       
        Console.WriteLine($"Hi my name is {name}. I am {age} years old. Recently I bought a bouquet of flowers for a girl for a price of {price} and it is {isLesbian.ToString().ToLower()} that i am a lesbian.");
    }
}





using System;

class Program
{
    static void Main()
    {
        
        string name = "Andrea";
        int age = 20;
        double money = 75.99;
        string orientation = "Lesbian";
        bool isLesbian = true;

        
        Console.WriteLine($"Hello, my name is {name} and I am {age} years old. " +
                          $"Right now, I have ${money} with me. " +
                          $"My sexual orientation is {orientation}, which is {isLesbian.ToString().ToLower()}.");
    }
}




